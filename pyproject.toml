[project]
name = "langgraph-crashcourse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain[openai]>=1.3.2",
    "langgraph>=1.2.2",
    "numpy>=2.4.6",
    "python-dotenv>=1.2.2",
]
